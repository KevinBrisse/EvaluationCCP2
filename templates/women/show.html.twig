{% extends 'base.admin.html.twig' %}

{% block title %}Women{% endblock %}

{% block body %}
    <h1>Women</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ woman.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ woman.image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ woman.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ woman.description }}</td>
            </tr>
            <tr>
                <th>Soustitre</th>
                <td>{{ woman.soustitre }}</td>
            </tr>
            <tr>
                <th>Icone</th>
                <td>{{ woman.icone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('women_index') }}">back to list</a>

    <a href="{{ path('women_edit', {'id': woman.id}) }}">edit</a>

    {{ include('women/_delete_form.html.twig') }}
{% endblock %}
